<div class="bg-slate-50">
  <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-6">
    <header class="space-y-2">
      <h1 class="text-2xl md:text-3xl font-extrabold tracking-tight text-slate-900">
        検索履歴
      </h1>
      <p class="text-sm text-slate-600">
        過去に保存した検索条件から、すぐ再検索できます。
      </p>
    </header>

    <% if @search_sessions.blank? %>
      <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
        <p class="text-slate-700 font-semibold">検索履歴はまだありません。</p>
        <p class="mt-2 text-sm text-slate-600">
          漢方検索を実行すると、ここに履歴が表示されます。
        </p>
      </div>
    <% else %>
      <section class="rounded-2xl border border-slate-200 bg-white shadow-sm overflow-hidden">
        <div class="px-6 py-4 border-b border-slate-200 flex items-center justify-between gap-3">
          <h2 class="text-sm font-semibold text-slate-900">履歴一覧</h2>
          <span class="text-xs font-semibold text-slate-600">
            全 <%= @search_sessions.size %> 件
          </span>
        </div>

        <ul class="divide-y divide-slate-200">
          <% @search_sessions.each do |ss| %>
            <li class="p-6 hover:bg-slate-50 transition">
              <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
                <div class="min-w-0 sm:flex-1">
                  <div class="flex flex-wrap items-center gap-3">
                    <span class="inline-flex items-center rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-900 border border-emerald-100">
                      ヒット <%= ss.result_count %>件
                    </span>

                    <span class="text-xs text-slate-500">
                      <%= ss.created_at.strftime("%Y/%m/%d %H:%M") %>
                    </span>
                  </div>

                  <% names = ss.result_kampo_names(limit: 3) %>
                  <% if names.present? %>
                    <p class="mt-3 text-sm text-slate-700">
                      <span class="font-semibold text-slate-900">漢方例：</span>
                      <%= names.join(" / ") %>
                    </p>
                  <% else %>
                    <p class="mt-3 text-sm text-slate-600">
                      <span class="font-semibold text-slate-900">漢方例：</span>
                      なし
                    </p>
                  <% end %>
                </div>

                <!-- 中：検索メモ（ここが揃う） -->
                <div class="shrink-0 sm:w-36">
                  <div class="w-full inline-flex flex-col items-center justify-center rounded-xl border border-slate-200 bg-slate-50 px-4 py-2 text-sm text-center">
                    <span class="font-semibold text-slate-700">検索メモ</span>
                    <% memo = ss.case_note&.body %>
                    <span class="text-slate-600"><%= memo.presence || "なし" %></span>
                  </div>
                </div>

                <div class="shrink-0">
                  <%= link_to "詳細へ",
                      search_session_path(ss),
                      class: "inline-flex items-center justify-center rounded-xl
                              border border-slate-300 bg-white
                              px-4 py-2 text-sm font-semibold text-slate-700
                              hover:bg-slate-50 transition" %>
                </div>
              </div>
            </li>
          <% end %>
        </ul>
      </section>
      <div class="pt-4">
        <%= paginate @search_sessions %>
      </div>
    <% end %>
  </div>
</div>
