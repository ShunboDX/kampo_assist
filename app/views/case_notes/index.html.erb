<p><%= link_to "新規作成", new_case_note_path %></p>

<h1>症例メモ一覧</h1>

<% if @case_notes.any? %>
  <ul>
    <% @case_notes.each do |note| %>
      <li>
        <%= simple_format(h(note.body)) %>
        <small>
          （<%= l(note.created_at, format: :short) rescue note.created_at.to_s %>）
        </small>

        <p>
          <%= link_to "編集", edit_case_note_path(note) %>
          <%= button_to "削除", case_note_path(note),
                        method: :delete,
                        data: { turbo_confirm: "削除しますか？" } %>
        </p>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>まだ症例メモがありません。</p>
<% end %>
